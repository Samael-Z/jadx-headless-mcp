# Rust
target/
debug/
**/*.rs.bk
*.pdb
**/mutants.out*/

# Java / Maven
bridge/target/
*.class
.mvn/wrapper/maven-wrapper.jar

# IDE
.idea/
.vscode/
*.iml

# OS
.DS_Store
Thumbs.db

# Local artifacts
/dist/
/release/
*.log

# Credentials / secrets — must NEVER be committed.
# .npmrc here is the local auth file dropped during manual `npm publish`;
# CI uses GitHub Secrets instead. If you see this file appear in `git status`,
# something has gone wrong — delete it before committing.
.npmrc
**/.npmrc

# Personal local-only dev notes (release cheat sheet, roadmap/status)
dev-readme.md
ROADMAP.md

# Vendored jar drop location (built locally; release uses CI artifact)
src/embedded/jadx-bridge.jar
